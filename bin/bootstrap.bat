docker-compose run --no-deps %1 bash -c "composer install && mkdir -p config/jwt && openssl genpkey -out config/jwt/private.pem -aes256 -algorithm rsa -pkeyopt rsa_keygen_bits:4096 && openssl pkey -in config/jwt/private.pem -out config/jwt/public.pem -pubout && rm var/test.db3 && bin/console doctrine:schema:create --env=test && /var/www/bin/phpunit --configuration /var/www/phpunit.xml.dist /var/www/tests"
